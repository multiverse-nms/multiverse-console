server {
    listen 443 ssl;
    server_name mnms.gui.com;    charset utf-8;
    root    /usr/share/nginx/html;
    index   index.html index.htm;    # Always serve index.html for any request

    ssl_certificate /etc/nginx/certs/mnms.gui.crt.pem;
    ssl_certificate_key /etc/nginx/certs/mnms.gui.key.pem;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
    
    error_log  /var/log/nginx/vue-app-error.log;
    access_log /var/log/nginx/vue-app-access.log;
}